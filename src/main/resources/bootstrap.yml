spring:
  application:
    name: conf
  cloud:
    config:
      enabled: true
      uri: http://localhost:8888
management:  endpoints:
    web:
      exposure:        include: "*"
      basePath: "/actuator"